# Lab 2 — Use the Python Interactive Shell

## Steps
1. Open a terminal.
2. Run `python --version` to verify the version.
3. Start REPL: `python`.
4. At the `>>>` prompt run `print('Hello, Network!')`.
5. Exit with `exit()` or Ctrl+D.

The runnable equivalent is `hello_network.py`.